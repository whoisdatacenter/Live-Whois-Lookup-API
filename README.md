# Live WHOIS Lookup API

Live WHOIS Lookup API by WhoisDataCenter.

Get real-time WHOIS information for any domain name, including registrar, registrant, registration dates, expiration date, name servers, status, contacts and raw WHOIS data.

## API Endpoint

```http
GET /api/v2/domain/whois
